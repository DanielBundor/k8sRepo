apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "danapp.fullname" . }}-test-connection"
  labels:
    {{- include "danapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "danapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
