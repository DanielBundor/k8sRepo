apiVersion: v1
kind: Service
metadata:
  name: {{ include "danapp.fullname" . }}
  labels:
    {{- include "danapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "danapp.selectorLabels" . | nindent 4 }}
